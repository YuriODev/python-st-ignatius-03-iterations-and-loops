# Prompt the user to enter the number of times to print the message
n = int(input("Enter the number of times to print the message: "))

# Use a for loop to repeat the following block of code 'n' times
for _ in range(n):
    # Print the message "Hello, Python!"
    print("Hello, Python!")
